global tk
global requestG
global responseG

param (...localParams)

pl("[%v] localParams: %v", getNowStr(), localParams)

pln("a1:", localParams[0].a1)
pln("a2:", localParams[0].a2)

result := toInt(localParams[0].a1) + toInt(localParams[0].a2)

// setRespHeader(responseG, "Content-Type", "text/json; charset=utf-8")

writeResp(responseG, toStr(result))

return "TX_END_RESPONSE_XT"