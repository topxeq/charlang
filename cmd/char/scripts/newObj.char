r1 := newObj("stringRing")

pl("r1 type: %v", typeName(r1))

pl("r1 type wrapped in any: %v", typeOfAny(r1))

method(r1, "Push", "abc")

method(r1, "Push", "123")

r2 := method(r1, "GetList")[0]

plt(r2)
